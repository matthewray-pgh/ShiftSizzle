import { 
  ContentPanel 
} from "../Components";

import "./Settings.scss";

export const Settings = () => {
  return (
    <div className="settings">
      <ContentPanel>
        <p>This is the settings view.</p>
      </ContentPanel>
    </div>
  );
}
